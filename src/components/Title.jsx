function Title({ userCount }) {
    return (
        <div>
            <h1 className="text-3xl mt-4">
                {userCount} Birthdays Today
            </h1>
        </div>
    )
}

export default Title